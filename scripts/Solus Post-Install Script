# IN PROGRESS # Terminal Commands History Log

sudo eopkg rm baobab firefox gnome-mpv hexchat libreoffice-impress libreoffice-draw libreoffice-calc libreoffice-writer libreoffice-common thunderbird rhythmbox-alternative-toolbar rhythmbox gnome-photos openconnect vpnc openvpn

sudo eopkg install -c system.devel

sudo eopkg it vivaldi-stable libinput-gestures gnome-tweaks ntfs-3g openjdk-8

sudo eopkg rmo

sudo gpasswd -a $USER input

libinput-gestures-setup autostart

cp /usr/share/libinput-gestures/libinput-gestures.conf .config/libinput-gestures.conf

sudo eopkg up

sudo snap install eclipse --classic

snap refresh
# Remove original directory: rm -r Music
# Symlink: ln -s /mnt/98B8BC91B8BC6F7E/"Study Drive"/Music /home/navtoj